# Alou

Alou is a lightweight developer toolbox packaged as a Debian package (.deb).

Build:

```sh
cd alou
./build_deb.sh
```

Install:

```sh
sudo dpkg -i alou.deb
```

After installing, open a NEW terminal to load aliases and functions.
